
// library for user input
#include <iostream>

// We shouldn't use this line
// It makes code smaller, but creates some danger for
// Naming errors
// using namespace std;

// This is a standard main function
// It would run o the start of the program
int main() {
  // std::cout << "Hello World!\n";
  // standard output for Hello World program
  std::cout << "Hello World!" << std::endl;
  // std::eddl -> more readable 
  
  // The program should return 0 as a signal for good completion
  return 0;
}
